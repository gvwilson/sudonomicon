# Session log — 2026-03-29T13:34:53Z

## Prompt

Read the existing tutorial content. Create `outline.txt` with a summary of
existing content, a critique of that content, suggestions for moving or
deleting existing content if necessary, and suggestions for 20 other topics
that could be added.

## Actions

1. Read `README.md` to get the lesson list and table of contents.
2. Read all lesson files:
   `intro/index.md`, `fs/index.md`, `proc/index.md`, `jobs/index.md`,
   `http/index.md`, `auth/index.md`, `var/index.md`, `packenv/index.md`,
   `dock/index.md`, `finale/index.md`.
3. Created `log/` directory (did not exist).
4. Wrote `outline.txt` containing:
   - Summary of each existing lesson's content.
   - Critique covering structural, organisational, and style problems,
     plus content gaps.
   - Recommendations for moving/deleting content (git hooks, virtual-env
     duplication, fork/exec, cron stub, fswatch).
   - 20 suggested new topics (SSH, HTTPS/TLS, sudo, sockets/ports, DNS,
     env-var security, pyproject.toml, databases in containers, logging,
     Makefiles, systemd, networking basics, compression, regex in shell,
     cron full lesson, IPC, REST APIs, config files, Docker Compose).

---

## Prompt (2026-03-29T13:45Z)

1. Continue appending to this log file.
2. Write a point-form outline of authentication on Unix and Windows in
   `auth/index.md`, at roughly the same length and detail as `proc/index.md`.
3. Outline a new tutorial `network/index.md` covering networking basics
   including DNS.
4. Find and describe alternatives to `cron` with better user interfaces;
   add that description to `outline.txt`.
5. Fill in the exit status section of `proc/index.md`.

## Actions

1. Read `glossary/index.md` and `_extras/links.md` for existing terms and
   link keys.
2. Filled in `proc/index.md` exit status section (replaced the FIXME stub):
   - Explained 0=success / non-zero=failure convention and why it's reversed
   - Common non-zero codes table (1, 2, 126, 127, 128+N)
   - Shell $? variable with examples
   - sys.exit() in Python
   - subprocess.run().returncode
   - && and || shell operators; connection back to Git hooks
   - Added two exercises
3. Rewrote `auth/index.md` (was a FIXME stub) with sections on:
   - The three A's revisited
   - /etc/passwd and user accounts on Unix
   - Password storage: hashing, salts, rainbow tables (hashlib/pbkdf2)
   - sudo and su (with /etc/sudoers examples)
   - Windows authentication (SAM, NTLM, Active Directory, UAC; comparison table)
   - SSH key pairs (ssh-keygen, ssh-copy-id, ssh-agent, ~/.ssh/config)
   - Data authentication: checksums, HMAC, timing attacks
   - Multi-factor authentication and TOTP (pyotp example)
4. Created new `network/index.md` with sections on:
   - What a network is
   - IP addresses (IPv4, IPv6, special addresses)
   - Ports (well-known ports table, netstat)
   - TCP vs. UDP (comparison)
   - Sockets in Python (tcp_server.py / tcp_client.py examples)
   - DNS: record types table, resolution process, /etc/hosts
   - Querying DNS with dig; reverse DNS
   - DNS in Python (socket module; dnspython)
   - Network troubleshooting (ping, traceroute, curl -I, lsof)
5. Appended "CRON ALTERNATIVES WITH BETTER USER INTERFACES" section to
   `outline.txt` covering: systemd timers, launchd, APScheduler, schedule,
   Prefect, Airflow, Dagster, GitHub Actions schedules, Temporal,
   healthchecks.io, Cronitor; with a recommendation for what to teach.
